package com.holderzone.hardware.camera

/**
 * Available runtime backends used by the SDK after backend resolution.
 */
enum class CameraBackend {
    CAMERA_X,
    CAMERA_2,
    CAMERA_1,
    UVC,
}
